namespace TicTacToe
{
    internal class TicTacToeDescription
    {
        /*
         * Develop a simple tic tac toe game for playing by users. Here are some mandatory items:
         * - when the game starts, users should see the gaming field with exact coordinates;
         * - after each step, users should see the existing field, as per their previous inputs;
         * - the system avoids selecting occupied point. 
         * 
         * Tips:
         * Create a matrix for the field.
         * Fill the matrix in order to track results.
         * Use methods from matrix task in order to verify diagonals.
         */
    }
}
